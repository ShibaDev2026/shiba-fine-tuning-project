# Layer 2 Backend runtime dependencies
# 版本對齊 Phase 1 驗證過的 host venv（Python 3.14），目標 runtime Python 3.11-slim
# 訓練相關依賴（mlx / transformers / numpy）不在此檔，Layer 3 獨立服務自己管

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.5
apscheduler==3.11.0
pyyaml==6.0.2
openai==1.59.6
